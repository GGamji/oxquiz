//module5.js => message.js

const message = (m) => {
  return m
}
export default message;
//여러 개를 내보내거나, 하나만 내보내더라도 하나의 목적을 가지지 않았다면 default가 아니라 {} 방식으로 사용

//default 내보내기는 하나만 내보내거나, 내보낼 자료(특히, 함수)의 목적을 명확하게하기 위해 모듈 파일을 하나만 사용하는 것을 선호

//export 방식에 따라 import 방식도 달라짐